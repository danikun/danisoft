<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="org.danisoft.Mapper.PhoneNumberMapper">
  
  	<insert id="save" flushCache="true" keyProperty="id" useGeneratedKeys="true" parameterType="PhoneNumber">
  		INSERT INTO 
  			PhoneNumber (phonenumber, phonenumbertype, contact) 
  			VALUES(#{number}, #{typeId}, #{contactId})
  	</insert>
  	
  	<update id="update" flushCache="true" parameterType="PhoneNumber">
  		UPDATE 
  			PhoneNumber
		SET
			phonenumber = #{number},
			phonenumbertype = #{type.id}
		WHERE
			id = ${id}
  	</update>
  	
  </mapper>